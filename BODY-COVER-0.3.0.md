# 正文封面 0.3.0

## 使用方式

通过 Discourse 正文工具栏原有的图片上传按钮上传图片。正文中第一张符合尺寸和归属要求的图片会成为列表封面。编辑器上方不再显示独立封面表单，话题页面不再额外显示一张封面。

后台搜索 `topic_cover_required`：开启时，普通话题的正文必须有有效图片；关闭时，允许无图发帖。`topic_cover_enabled` 控制整体功能。默认最小尺寸仍为 480×270，可在后台调整。

正文里的图片随 Discourse 自带审核 payload 保存，批准创建时再从正文生成列表封面，无需单独传封面 ID。普通回复和私信不执行主帖封面要求。编辑主帖时，删除最后一张有效图片会在强制模式下被拒绝；可选模式下保存后清空列表封面字段。

沿用 Discourse 管理员审核批准等内部流程的 skip_validations 行为。通过普通发帖和编辑请求无法只传一个旧封面 ID 来绕过正文检查。

## 更新方式

这是现有插件的更新包。将包内文件按原相对路径覆盖到构建使用的源码仓库，提交并推送，再在 `/var/discourse` 执行 `./launcher rebuild app`。部署后刷新浏览器再发帖。

旧上传框和额外封面展示文件保留为空组件，确保覆盖更新时旧组件不会残留生效。原独立 PUT /topic-cover/:topic_id 路由已停用。不要混用先前版本的 plugin.rb 或 assets 文件。

旧帖子不会自动补正文图片。旧帖原本只存独立封面、正文无图时，升级后话题页面不再额外展示该图；原列表封面在编辑前仍可保留。编辑该主帖时需按新规则在正文中上传图片。

## 验证

新增/更新了 9 个 RSpec 用例，覆盖正文取图、必填开关、伪造旧字段、链接和代码块排除、归属、尺寸筛选、审核批准和编辑删除。

本机没有可用的完整 Discourse/RSpec 测试环境。该版本的 RSpec、完整前端构建和浏览器端发帖审核尚未运行；此前旧版测试通过结果不能作为此版本验证结果。请先在测试环境验证再部署正式站点。

已做代码差异检查；Ruby 语法检查结果见交付说明。此包未由助手部署或推送。
