# 独立强制封面按钮 0.4.0

保留独立“上传封面”按钮。上传后自动把指定图片放在正文第一行，再次上传替换之前的指定封面，其他正文图片保留。图片随正文进入审核；列表使用同一封面；话题页面不额外渲染第二张封面。

插件启用时，发帖必须有按钮生成的首行封面。后端检查首行标记、真实图片上传 ID、归属、尺寸及唯一性，不能仅传旧封面字段。删除正文封面或移动到非首行后须重新上传。已取消旧的可选封面设置，强制规则始终生效（整体停用插件除外）。

首行使用 `![topic-cover-上传ID](upload://...)`，请保留此标记。编辑既有话题也通过按钮重新选择封面；正文保存后更新列表。旧版无标记正文首次编辑需重新使用按钮上传。

代码更新在独立工作区。按相对路径覆盖插件源码并提交、推送至构建使用的 Git 分支后，执行 `/var/discourse/launcher rebuild app`（工作目录为 `/var/discourse`）。本包不是对服务器的自动部署。

已执行前端纯函数的8项断言。RSpec已更新，但现有环境没有完整测试依赖，尚未执行；完整前端构建、浏览器上传和真实审核批准仍需测试环境验证。交付包不采用之前版本的测试结论。
