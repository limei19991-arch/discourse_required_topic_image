# 首次上传修复 0.4.1

生产日志中，同一封面 ID 30 的第一次发帖于 2026-09-05 10:15:19 UTC 返回 422，正文图片 Markdown 与后续文字没有换行；第二次于 10:15:38 UTC 返回 200，两者之间已补上两个换行。10:21 的另一组请求复现同一差异。

原因是空正文上传分支仅返回图片 Markdown，没有结尾段落分隔符。首次上传后继续输入，文字接到了封面同一行，未通过后端首行匹配。

修复：无论正文是否为空，上传后都输出封面 Markdown、两个换行和正文。更换旧封面时收紧匹配范围，避免误吞与图片粘在同一行、且带括号的正文文字。

独立上传按钮、首行标记、唯一性、图片归属、尺寸、强制上传和审核逻辑全部保留。该版本未改后端校验逻辑。

验证：Node 回归断言先在旧实现失败，再在修复后通过，覆盖首次上传后输入正文、替换、重复上传、旧草稿和保留其他正文图片。完整前端构建和浏览器端发布尚未执行；本次不部署正式服务器。

更新源码仓库并推送后，在 /var/discourse 执行 ./launcher rebuild app，刷新浏览器。已打开的旧草稿如封面与文字粘在一行，可直接在封面后按两次回车，无须重新上传图片。
