# 封面审核队列修复

此补丁只修复进入待审核流程时封面 ID 丢失的问题，不调整站点设置。

本次更新还保留现有上传按钮，并在上传完成后自动把图片插入正文开头，供审核正文直接显示。列表仍使用同一图片的封面 ID。新建帖子中更换上传图片时，只替换编辑器记录的那一处图片；记录随草稿保存。不会自动修改旧帖正文。编辑已有话题时，封面接口成功后更新编辑器正文，正文仍需用户保存。

本次包还包含 `assets/` 和 `config/`，部署时必须一并更新。正文插图辅助函数通过 7 项 Node 断言；完整 Discourse 前端构建和浏览器测试尚未执行。此变更不增加“删除正文图片后阻止发帖”的额外校验。

## 文件

- 替换插件根目录的 `plugin.rb`。
- 新增 `lib/discourse_topic_cover/queued_post_support.rb`。
- `spec/plugin_spec.rb` 是回归测试，不是运行时依赖。

## 原理

将已经过控制器过滤的 `topic_opts` 保存进审核 payload；从 JSON 恢复创建参数时规范化嵌套键；审核记录保存后补充封面上传引用，不删除正文附件引用。

## 部署

1. 在插件源码仓库中备份原版本，按压缩包内相对路径更新上述文件。
2. 提交并推送到 `containers/app.yml` 中实际使用的插件仓库和分支。
3. 在 Discourse 主机的 `/var/discourse` 执行 `./launcher rebuild app`。
4. 用 TL0、无历史帖子的普通成员快速提交带有效封面的首帖，检查能否进入待审核状态；批准后检查话题封面。

仅修改运行中容器内的文件不会保证下次 rebuild 保留改动。此代码尚未推送到 GitHub，需先更新构建所用的源码。

## 验证范围

支持模块已通过真实 NewPostManager.enqueue 调用、JSON 参数恢复、上传引用检查以及 PostCreator 模拟批准创建的回归脚本。Ruby 支持模块语法检查通过。

完整 RSpec 未运行：现有镜像不含 rspec 可执行程序。未完成浏览器端实际审核批准测试，也未在正式服务部署。测试脚本使用临时进程手工注册模块，因此不能替代完整插件启动验证。

修复只对部署后新提交的请求生效；已丢失封面 ID 的旧审核记录无法自动推断原封面。

## 回退

部署前记录插件仓库版本。需要回退时，在源码仓库撤销本次修复提交，推送后重新 rebuild。删除本地工作分支本身不会回退已经部署的服务器。
